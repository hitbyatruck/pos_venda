# Stage all the deleted files
git add --all

# Commit the changes with a descriptive message
git commit -m "Project cleanup: removed migration backups and screenshot files"

# Push the changes to your remote branch (optional)
git push origin navegacao-unificada
